DATABASE_URL="mongodb+srv://YOUR_USER:YOUR_PASS@YOUR_CLUSTER.mongodb.net/padhaaro-crm"

# Signs session cookies. Generate one with:  openssl rand -base64 32
# Required in production — the app refuses to sign sessions without it.
AUTH_SECRET=""

# Owner login. Kept here (and in the Vercel dashboard) rather than in the
# database seed, so no credential is ever committed to the repo.
ADMIN_EMAIL=""
ADMIN_PASSWORD=""
ADMIN_NAME=""
